{
  "name": "adonis-hexa",
  "version": "3.0.0",
  "description": "A cli tool for adonis hexa.",
  "main": "index.js",
  "bin": "app/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-node": "babel-node --presets=es2015",
    "build": "rimraf dist/ && babel app --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "dev-server": "nodemon --exec npm run babel-node && standard -- app/index.js ",
    "start": " npm run build && node dist/index.js",
    "lint": "standard --fix"
  },
  "author": "Emmanuel ogbiyoyo",
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.18.4",
    "rimraf": "^2.6.2",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "commander": "^2.19.0",
    "cross-spawn": "^6.0.5",
    "eslint-plugin-import": "^2.14.0",
    "figlet": "^1.2.1",
    "kleur": "^2.0.2",
    "minimist": "^1.2.0",
    "nodegit": "^0.22.2",
    "shelljs": "^0.8.2"
  }
}
